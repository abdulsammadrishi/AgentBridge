module.exports = { id: 'demo-store-products', supports: ['search_products', 'get_product', 'check_inventory', 'add_to_cart'], requiresConfig: false, risk: 'low', requiresConfirmation: false, requiresConfirmationFor: tool => tool === 'add_to_cart', defaultConfig: () => ({}), validate: () => true, execute: ({ tool }) => ({ adapter: 'demo-store-products', tool, message: 'Execution is handled by the existing demo-store page runtime.' }) };
