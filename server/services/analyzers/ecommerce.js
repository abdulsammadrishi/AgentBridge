const { has, cap } = require('./generic');
function ecommerceCapabilities({ signals, isKnownDemo }) { const results = [], schemaProduct = signals.schemas.some(type => /product|offer/i.test(type)), productText = has(signals, /product|shop|catalog|\$\s*\d|price/), labels = { search_products: 'Search products', get_product: 'Product details', check_inventory: 'Check inventory', add_to_cart: 'Add to cart' }, adapter = (name, evidence) => cap(name.replace(/_/g, '-'), labels[name], name === 'add_to_cart' ? 'ACTION' : 'READ', name === 'add_to_cart' ? 'medium' : 'low', name, 0.99, evidence, 'adapter_available'); if (isKnownDemo) return [adapter('search_products', ['Known AgentBridge demo-store adapter', 'Catalog search implementation available']), adapter('get_product', ['Known AgentBridge demo-store adapter', 'Product detail implementation available']), adapter('check_inventory', ['Known AgentBridge demo-store adapter', 'Inventory implementation available']), adapter('add_to_cart', ['Known AgentBridge demo-store adapter', 'Cart implementation available'])]; const search = signals.inputs.some(input => /search/.test(input)); if (search && productText) results.push(cap('search-products', 'Search products', 'READ', 'low', 'search_products', schemaProduct ? 0.9 : 0.78, [schemaProduct ? 'schema.org/Product or Offer detected' : 'Product/catalog language detected', 'Search input detected'])); if (schemaProduct || productText) results.push(cap('get-product', 'Product details', 'READ', 'low', 'get_product', schemaProduct ? 0.86 : 0.61, [schemaProduct ? 'schema.org/Product detected' : 'Product/catalog language detected'])); if (has(signals, /\$\s*\d|price|sale price/)) results.push(cap('get-price', 'Get price', 'READ', 'low', 'get_price', 0.82, ['Price markup or currency value detected'])); if (has(signals, /in stock|out of stock|availability|inventory/)) results.push(cap('check-inventory', 'Check inventory', 'READ', 'low', 'check_inventory', 0.8, ['Availability or inventory language detected'])); if (has(signals, /add to (cart|bag|basket)/)) results.push(cap('add-to-cart', 'Add to cart', 'ACTION', 'medium', 'add_to_cart', 0.9, ['Add-to-cart CTA detected'])); if (has(signals, /checkout|place order|complete purchase/)) results.push(cap('place-order', 'Checkout / place order', 'ACTION', 'high', 'place_order', 0.86, ['Checkout or order CTA detected'])); return results; }
module.exports = { ecommerceCapabilities };
